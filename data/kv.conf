# Request definiation
r = sub, obj, act

# Policy definition
p = sub, obj, act

# Effect definition
e = some(where (p.eft == allow))

# Matcher definition
m = r.sub == p.sub && r.obj == p.obj && r.act == p.act